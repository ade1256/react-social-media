import styled from "styled-components";

export const AlbumItemContainer = styled.div`
  position: relative;
  margin-top: 24px;
  .album-item {
    display: flex;
    flex-wrap: wrap;
    margin-top: 8px;
    .thumbnail {
      margin: 4px;
    }
  }
`