import styled from "styled-components";

export const LayoutContainer = styled.div`
  position: relative;
  width: 800px;
  margin: 0 auto;
  .navbar {
    padding: 16px 0px;
    span {
      &:hover {
        cursor: pointer;
      }
    }
  }
`